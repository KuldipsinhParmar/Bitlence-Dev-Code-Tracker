# VS Code Dev Code Tracker extension data
.devCodeTracker/

# macOS
.DS_Store

# Node / npm (if ever used for build tooling)
node_modules/

# Packaged archives
*.zip

# Claude Code settings (dev tool, not part of the plugin)
.claude/

# Local Docker dev environment
docker-compose.yml

# Composer dev dependencies (PHPUnit test suite) — not required to run the plugin
vendor/

# Local PHPUnit DB config — copy tests/wp-tests-config-sample.php to create this
tests/wp-tests-config.php
.phpunit.result.cache

# Clean-build output for Plugin Check / WP.org packaging — generated by bin/build.sh
build/
