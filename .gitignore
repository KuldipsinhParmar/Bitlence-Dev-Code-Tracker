# VS Code Dev Code Tracker extension data
.devCodeTracker/

# macOS
.DS_Store

# Node / npm (if ever used for build tooling)
node_modules/

# Packaged archives
*.zip

# Claude Code settings (dev tool, not part of the plugin)
.claude/

# Local Docker dev environment
docker-compose.yml
